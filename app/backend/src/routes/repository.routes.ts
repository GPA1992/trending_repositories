import express from 'express';
import * as repositoryController from '../controllers/respository.controller';

const router = express.Router();


// R -> READ
router.get('/',);

export default router;
